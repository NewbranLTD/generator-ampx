const assert = require('assert');
// Const generatorAmpx = require('../index.js');

describe('generatorAmpx', () => {
  it('has a test', () => {
    assert(false, 'generatorAmpx should have a test');
  });
});
